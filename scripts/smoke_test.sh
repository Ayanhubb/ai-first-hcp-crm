#!/usr/bin/env bash
# Smoke test placeholder (scaffold) — fill when health endpoints exist
set -euo pipefail

BASE_URL="${BASE_URL:-http://localhost:8000}"
echo "Smoke test not implemented yet. Expected: GET ${BASE_URL}/health/live"
exit 0
